# 프레임워크 및 소스코드 변경 이력

## 1. 개요
* **작성 목적:** JEUS 7 ➔ JEUS 8 업그레이드 대응을 위한 하드코딩 로그 경로의 설정값 외부화(Properties) 및 관련 소스코드/설정파일 변경 내역 기록
* **작성일:** 2026.06.15

---

## 2. 변경 파일 및 세부 내역

### ① 설정 파일 변경
#### [MODIFY] [nhepro.properties (Front)](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/src/main/resources/nhepro.properties)
* **변경 내용:**
  Front 서버의 JEUS 로그 조회 경로 설정 추가
  ```properties
  ################################################################################
  # JEUS Server Log Paths
  ################################################################################
  # Front Server Log Path (JEUS 7 -> JEUS 8 업그레이드 시 경로 수정 필요)
  eversrm.log.path.front=/logs/tmax/jeus7/epcs11/JeusServer.log
  ```

#### [MODIFY] [nhepro.properties (Operator)](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProOperator/src/main/resources/nhepro.properties)
* **변경 내용:**
  Operator 서버의 JEUS 로그 조회 경로 설정 추가 (각 인스턴스별 구분)
  ```properties
  ################################################################################
  # JEUS Server Log Paths
  ################################################################################
  # Operator Server Log Paths (JEUS 7 -> JEUS 8 업그레이드 시 경로 수정 필요)
  eversrm.log.path.op.epop11=/logs/tmax/jeus7/epop11/JeusServer.log
  eversrm.log.path.op.epop21=/logs/tmax/jeus7/epop21/JeusServer.log
  eversrm.log.path.op.epcs11=/logs/tmax/jeus7/epcs11/JeusServer.log
  eversrm.log.path.op.epcs21=/logs/tmax/jeus7/epcs21/JeusServer.log
  ```

---

### ② 소스코드 변경 및 주석 추가
#### [MODIFY] [WatcherController.java (Front)](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/src/main/java/com/st_ones/common/eversrm/system/WatcherController.java#L38)
* **변경 내용:**
  기존 하드코딩된 `/logs/tmax/jeus7/...` 경로 대신 `PropertiesManager.getString("eversrm.log.path.front")`를 호출하도록 로직을 개선하고 변경 관련 고도화 주석 추가.
  ```java
  // [고도화변경] 2026.06.15 JEUS 7 -> JEUS 8 업그레이드 대응을 위해 하드코딩된 로그 경로를 nhepro.properties로 이관
  String path = PropertiesManager.getString("eversrm.log.path.front");
  ```

#### [MODIFY] [WatcherController.java (Operator)](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProOperator/src/main/java/com/st_ones/common/eversrm/system/WatcherController.java#L38-L49)
* **변경 내용:**
  기존 각 분기 조건마다 하드코딩된 `/logs/tmax/jeus7/...` 경로 대신 `PropertiesManager.getString("eversrm.log.path.op.[인스턴스]")`를 각각 호출하도록 개선하고 변경 관련 고도화 주석 추가.
  ```java
  // [고도화변경] 2026.06.15 JEUS 7 -> JEUS 8 업그레이드 대응을 위해 하드코딩된 로그 경로를 nhepro.properties로 이관
  if (reqMap.get("logType").equals("O")) {
      if ("1".equals(reqMap.get("actionType"))) {
          pathSize[0] = PropertiesManager.getString("eversrm.log.path.op.epop11");
      } else {
          pathSize[0] = PropertiesManager.getString("eversrm.log.path.op.epop21");
      }
  } else {
      if ("1".equals(reqMap.get("actionType"))) {
          pathSize[0] = PropertiesManager.getString("eversrm.log.path.op.epcs11");
      } else {
          pathSize[0] = PropertiesManager.getString("eversrm.log.path.op.epcs21");
      }
  }
  ```

---

## 3. 검증 및 빌드 확인 사항
* JEUS 8 업그레이드 배포 후 `nhepro.properties` 파일 내의 `eversrm.log.path.*` 값을 신규 JEUS 8 로그 경로로 변경 후 기능(로그 뷰어)이 정상 동작하는지 테스트가 필요합니다.
* 변경된 속성을 읽어오는지 WAS 기동 로그 및 로그 뷰어 동작 여부로 직접 확인 가능합니다.

---

## 4. Maven 의존성 최적화 (중복 및 미사용 라이브러리 제거)
* **작성 목적:** 프로젝트 내 불필요하고 중복 정의된 Maven 라이브러리(Dependencies) 정리 및 빌드 최적화
* **작성일:** 2026.06.15
* **변경 내용:**
  - **공통 변경 파일:**
    - [NHeCommon/pom.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeCommon/pom.xml)
    - [NHeProFront/pom.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/pom.xml)
    - [NHeProOperator/pom.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProOperator/pom.xml)
  - **세부 변경 내역:**
    - **중복 정의 제거:**
      - `net.sourceforge.jexcelapi:jxl` (각 pom.xml에 2번씩 중복 정의되어 있던 1개 선언 삭제)
    - **미사용 라이브러리 삭제:**
      - `org.apache.commons:commons-digester3` (2개 선언 모두 미사용 확인 후 삭제)
      - `net.sf.json-lib:json-lib` (Jackson/Gson 대체로 미사용 삭제)
      - `org.apache.commons:commons-email` (미사용 삭제)
      - `commons-configuration:commons-configuration` (미사용 삭제)
      - `commons-net:commons-net` (미사용 삭제)
      - `args4j:args4j` (미사용 삭제)
      - `com.sun.mail.smtp:smtp` (system scope smtp 삭제, `javax.mail` 패키지에서 기본 지원)
    - **NHeProFront 전용 미사용 라이브러리 삭제:**
      - `org.projectlombok:lombok` (Lombok 어노테이션 미사용 확인 후 삭제)
  - **유지 라이브러리 (요청 반영):**
    - `sapjco:sapjco` (SAP Java Connector - 사용 여부와 무관하게 기존 설정 유지)

---

## 5. pom.xml SNAPSHOT 버전 변경 (운영 배포용)
* **작성 목적:** 운영 서버 배포를 위해 프로젝트 내 모든 `pom.xml` 파일의 `1.0-SNAPSHOT` 버전을 SNAPSHOT이 제외된 릴리즈 버전인 `1.0.0`으로 변경 및 빌드 검증 수행
* **작성일:** 2026.06.15
* **변경 내용:**
  - **공통 변경 파일:**
    - [NHeCommon/pom.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeCommon/pom.xml)
    - [NHeProFront/pom.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/pom.xml)
    - [NHeProOperator/pom.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProOperator/pom.xml)
  - **세부 변경 내역:**
    - **NHeCommon**: 자체 프로젝트 버전 정보 수정 (`1.0-SNAPSHOT` ➔ `1.0.0`)
    - **NHeProFront**: 자체 프로젝트 버전 정보 수정 (`1.0-SNAPSHOT` ➔ `1.0.0`) 및 `NHeCommon` 의존성(dependency) 버전 정보 수정 (`1.0-SNAPSHOT` ➔ `1.0.0`)
    - **NHeProOperator**: 자체 프로젝트 버전 정보 수정 (`1.0-SNAPSHOT` ➔ `1.0.0`) 및 `NHeCommon` 의존성(dependency) 버전 정보 수정 (`1.0-SNAPSHOT` ➔ `1.0.0`)
  - **빌드 및 배포 검증:**
    - `NHeCommon`: `mvn clean install -DskipTests` 빌드 성공 (로컬 `.m2` 리포지토리에 `NHeCommon-1.0.0.jar` 설치 완료)
    - `NHeProFront`: `mvn clean package -DskipTests` 빌드 성공 (`NHeProFront-1.0.0.war` 패키징 완료)
    - `NHeProOperator`: `mvn clean package -DskipTests` 빌드 성공 (`NHeProOperator-1.0.0.war` 패키징 완료)

---

## 6. Maven systemPath 변수명 변경 (빌드 검증 로그 에러 제거)
* **작성 목적:** Maven 3.x 이상 빌드 시 `<systemPath>` 경로 내 `${basedir}` 변수 사용으로 인해 모델 검증 단계에서 발생하는 `[ERROR] ... must specify an absolute path` 경고/오류 로그를 제거하고 정석적인 절대 경로 변수 사용으로 개선
* **작성일:** 2026.06.15
* **변경 내용:**
  - **공통 변경 파일:**
    - [NHeCommon/pom.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeCommon/pom.xml)
    - [NHeProFront/pom.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/pom.xml)
    - [NHeProOperator/pom.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProOperator/pom.xml)
  - **세부 변경 내역:**
    - 각 모듈의 `pom.xml` 파일에 정의된 로컬 라이브러리(`system` 범위 의존성)들의 `<systemPath>` 경로를 기존 `${basedir}`에서 Maven 공식 권장 값인 **`${project.basedir}`**로 일괄 치환
      - 변경 전: `<systemPath>${basedir}/src/main/webapp/WEB-INF/lib/...`
      - 변경 후: `<systemPath>${project.basedir}/src/main/webapp/WEB-INF/lib/...`
  - **효과:**
    - Maven의 POM 모델 분석 및 빌드 수행 시 `[ERROR]` 수준의 불필요한 절대 경로 미지정 검증 경고 로그가 깔끔하게 제거됨을 확인

---

## 7. EverConverter.java 성능 최적화 및 보안 취약점 조치
* **작성 목적:** 프레임워크의 공통 유틸리티인 `EverConverter` 클래스의 반복 객체 생성 제거 및 루프 범위 오염으로 인한 SQL Injection 우회 취약점 해결
* **작성일:** 2026.06.16
* **변경 내용:**
  - **변경 파일:**
    - [EverConverter.java](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeCommon/src/main/java/com/st_ones/common/util/clazz/EverConverter.java)
  - **세부 변경 내역:**
    - **ObjectMapper 인스턴스 싱글톤 관리**: JSON 직렬화/역직렬화 메서드 호출 시마다 무겁고 스레드 안전한 `ObjectMapper`를 중복 생성하던 로직을 `static final` 멤버 필드 캐싱 구조로 최적화.
    - **SQL Injection 방어 우회 취약점 패치**: `forDynamicQuery` 내부에서 `isIgnoreCheckInjection` 플래그 스코프가 루프 외부로 지정되어, 하나의 예외 필드가 나오면 이후 모든 필드가 검사를 우회하던 심각한 보안 결함을 해결하고, `Set<String>`의 `O(1)` 탐색 기반으로 구조화.
    - **makeDynamicStatement 구조 및 데이터베이스 연산자 캐싱**: `Map`의 잦은 생성을 `switch-case` 구문으로 교체하여 O(1) 수준으로 응답성을 올렸으며, DB 벤더별 병합 연산자 지연 초기화 및 `key` 길이 체크 방어 코드 추가.
    - **정규식 기반 일괄 치환 패턴 프리컴파일**: 대소문자 혼용 SQL 예약어 및 스크립트 제거 정확도를 확보하기 위해 `Pattern.CASE_INSENSITIVE` 플래그와 함께 `Pattern` 인스턴스 상수를 정의하여 `convertInjection` 성능 최적화.
    - **StandardCharsets 적용**: `nullChk` 메서드의 하드코딩된 `"UTF-8"`을 `StandardCharsets.UTF_8.name()`으로 변경하여 표준 규격으로 일원화.

---

## 8. PMS 연계 인터페이스 성능 최적화 및 안정성 조치
* **작성 목적:** PMS 연계 인터페이스 모듈의 자원 사용 최적화, Null/NPE 등의 오류 방지 및 SQL 쿼리 안정성 보완
* **작성일:** 2026.06.16
* **변경 내용:**
  - **변경 파일:**
    - [PmsController.java](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/src/main/java/com/st_ones/nosession/interfacez/web/PmsController.java)
    - [PmsService.java](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/src/main/java/com/st_ones/nosession/interfacez/service/PmsService.java)
    - [PmsMapper.xml](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/src/main/resources/mappers/com/st_ones/nosession/interfacez/PmsMapper.xml)
  - **세부 변경 내역:**
    - **명명 규칙 준수**: `PmsController.java` 내 `pmsservice` 필드명을 표준 카멜케이스 규격인 `pmsService`로 수정.
    - **ObjectMapper 중복 생성 제거**: `PmsService.java` 내 `new ObjectMapper()`를 생성하여 JSON을 파싱하던 로직을 `EverConverter.readJsonObject` 및 타입 체크 구조로 변경하여 객체 재사용 최적화.
    - **방어 코드 및 Null 오류 방지**: 파라미터가 null이거나 `MSTDATA`가 누락/빈 리스트인 경우 조기 리턴을 유도하고, MyBatis 결과 `chk` 및 `RMK_TEXT` null 처리를 보완하여 NPE/IndexOutOfBoundsException을 방지.
    - **SQL 수치형 및 산출 필드 NVL 보완**: `PmsMapper.xml` 내 `insPrdt` 및 `insPmsPrdt` 쿼리의 `PR_AMT` 및 `ITEM_AMT` 수치 연산에 `NVL` 처리를 도입하여 수량/단가 누락 시 SQL 연산 오류 방지.

---

## 9. ERP 계약 전송 인터페이스 성능 최적화 및 안정성 조치
* **작성 목적:** ERP 계약 정보 전송 인터페이스 모듈의 자원 누수 방지, 파라미터 인코딩 오류 수정, 데이터 손상 버그 해결 및 매핑 성능 개선
* **작성일:** 2026.06.16
* **변경 내용:**
  - **변경 파일:**
    - [ContSendErpService.java](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/src/main/java/com/st_ones/nosession/interfacez/service/ContSendErpService.java)
  - **세부 변경 내역:**
    - **자원 누수 방지**: HTTP 스트림 대상 try-with-resources 적용 및 `finally` 블록에서 `http.disconnect()` 호출을 통한 웹 소켓/커넥션 리소스 해제 보장.
    - **URL 인코딩 및 중복 구문 수정**: `SEND_DATA` 쿼리 파라미터 전송 시 `URLEncoder.encode`를 통한 특수문자 전송 오류 방지 및 중복 세미콜론(;;) 제거.
    - **JSON "null" 문자열 치환 버그 조치**: 기존 JSON 직렬화 결과 문자열에서 `"null"` 단어를 일괄 치환하던 방식(예: `"annulment"` 등 실제 단어가 깨지는 문제)을 개선하여, Java Map 수준에서 루프를 돌며 null을 빈 문자열(`""`)로 치환한 뒤 직렬화하도록 변경.
    - **매핑 최적화**: 50줄의 수동 매핑 코드를 String 배열 루프로 축소하여 최적화 및 `new ObjectMapper()` 제거 후 `EverConverter`를 활용하도록 조치.

---

## 10. IT포탈 연계 인터페이스 성능 최적화 및 안정성 조치
* **작성 목적:** IT포탈 연계 인터페이스 모듈의 자원 누수 해제, 복사 루프 개선을 통한 성능 향상 및 각종 예외 방지 (NPE, NumberFormatException, StringIndexOutOfBoundsException)
* **작성일:** 2026.06.16
* **변경 내용:**
  - **변경 파일:**
    - [BNH0011_Service.java](file:///c:/ST-onesIDE/workspace/NHEPRO/NHeProFront/src/main/java/com/st_ones/nosession/interfacez/service/BNH0011_Service.java)
  - **세부 변경 내역:**
    - **자원 누수 해제 및 안정화 (try-with-resources 도입)**: `fileWrite` (EUC-KR 쓰기), 첨부파일 ZIP 압축, `copyFile` (채널 복사) 내 모든 `FileInputStream`, `FileOutputStream`, `BufferedWriter`, `FileWriter` 입출력 개체들을 try-with-resources 구문으로 묶어 예외 발생 유무와 무관하게 안전한 리소스 Close를 보장.
    - **데이터 매핑 알고리즘 속도 개선 및 예외(CME) 방지**: `changeRegUserId` 내부의 다중 루프를 통한 O(N) 리스트 검색 구조를 O(1) Map 구조로 최적화하고, 순회 도중 맵을 수정하여 발생할 수 있는 `ConcurrentModificationException`을 사전 차단하기 위해 임시 업데이트 맵을 통한 일괄 복사(`putAll`)로 개선.
    - **NullPointerException 가드**: BIZ_TYPE 비교 시 상수를 앞으로 배치(`"EC".equals(...)`), 리스트 null 체크 및 `makeData` 가드 처리를 강화하여 NPE 원천 차단.
    - **수치 및 인덱스 예외 가드**: 계약 금액 파싱 시 에러가 나지 않도록 `getSafeLong` 방어 함수를 구현하였고, `fileSq` 서브스트링 시 `StringIndexOutOfBoundsException` 방지 코드 추가 및 형변환 예외 방지를 위해 `formData` 복사 처리.



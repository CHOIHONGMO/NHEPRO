<?xml version="1.0" encoding="UTF-8" ?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
     http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

    <bean id="dataSourceSpied" class="org.apache.commons.dbcp.BasicDataSource">
        <property name="driverClassName" value="com.microsoft.sqlserver.jdbc.SQLServerDriver" />
        <property name="url" value="jdbc:sqlserver://dev.st-ones.co.kr:1435;databaseName=EVERMP2" />
        <property name="username" value="evermp2" />
        <property name="password" value="evermp2_01" />

        <property name="initialSize" value="10" /> <!-- The initial number of connections that are created when the pool is started. --><!-- Since: 1.2 -->
	    <property name="testOnReturn" value="true" /> <!-- The indication of whether objects will be validated before being returned to the pool. --> <!-- NOTE - for a true value to have any effect, the validationQuery parameter must be set to a non-null string. -->
	    <property name="maxActive" value="30" /><!-- The maximum number of active connections that can be allocated from this pool at the same time, or negative for no limit. -->
        <property name="maxIdle" value="15" /><!-- The maximum number of connections that can remain idle in the pool, without extra ones being released, or negative for no limit. -->
        <property name="maxWait" value="-1" /><!-- The maximum number of milliseconds that the pool will wait (when there are no available connections) for a connection to be returned before throwing an exception, or -1 to wait indefinitely. -->
        <property name="testOnBorrow" value="true" /><!-- The indication of whether objects will be validated before being borrowed from the pool. If the object fails to validate, it will be dropped from the pool, and we will attempt to borrow another. --> <!-- NOTE - for a true value to have any effect, the validationQuery parameter must be set to a non-null string. -->
	    <property name="testWhileIdle" value="true" /><!-- The indication of whether objects will be validated by the idle object evictor (if any). If an object fails to validate, it will be dropped from the pool. --><!-- NOTE - for a true value to have any effect, the validationQuery parameter must be set to a non-null string. -->
	    <property name="removeAbandoned" value="true" /><!-- Flag to remove abandoned connections if they exceed the removeAbandonedTimout. --> <!-- If set to true a connection is considered abandoned and eligible for removal if it has been idle longer than the removeAbandonedTimeout. Setting this to true can recover db connections from poorly written applications which fail to close a connection. -->
	    <property name="removeAbandonedTimeout" value="60" /><!-- Timeout in seconds before an abandoned connection can be removed. -->
        <property name="logAbandoned" value="true" /><!-- Flag to log stack traces for application code which abandoned a Statement or Connection. --> <!-- Logging of abandoned Statements and Connections adds overhead for every Connection open or new Statement because a stack trace has to be generated. -->
	    <property name="timeBetweenEvictionRunsMillis" value="60000" /><!-- The number of milliseconds to sleep between runs of the idle object evictor thread. When non-positive, no idle object evictor thread will be run. -->
        <property name="minEvictableIdleTimeMillis" value="60000" /><!-- The minimum amount of time an object may sit idle in the pool before it is eligable for eviction by the idle object evictor (if any). -->
    </bean>

    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="mapperLocations" value="classpath*:mappers/com/st_ones/**/*.xml"/>
        <property name="configLocation" value="classpath:mybatis/sqlmap-config-mssql.xml"/>
        <property name="databaseIdProvider" ref="databaseIdProvider"/>
    </bean>
</beans>
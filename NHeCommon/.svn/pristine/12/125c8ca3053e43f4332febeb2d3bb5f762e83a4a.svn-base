package com.st_ones.common.schedule;

import org.springframework.stereotype.Service;

/**
 * <pre>
 ******************************************************************************
 * 상기 프로그램에 대한 저작권을 포함한 지적재산권은 ㈜에스티원즈에 있으며,
 * ㈜에스티원즈가 명시적으로 허용하지 않은 사용, 복사, 변경, 제3자에의 공개, 배포는 엄격히 금지되며,
 * ㈜에스티원즈의 지적재산권 침해에 해당됩니다.
 * (Copyright ⓒ 2013 ST-ONES CORP., ALL RIGHTS RESERVED | Confidential)
 ******************************************************************************
 * </pre>
 * @File Name : MailScheduler.java
 * @date 2013. 07. 22.
 * @version 1.0
 * @see
 */
@Service
public class MailScheduler {

//    @Value("${eversrm.system.developmentFlag}")
//    private boolean devFlag;
//
//    @Autowired
//    JavaMailSender javaMailSender;
//
//    @Autowired
//    SchedulerMapper schedulerMapper;
//
//    @Autowired
//    LargeTextService largeTextService;
//
//    public void sendMail() throws Exception {
//
//        String mailAddrForDev = PropertiesManager.getString("eversrm.system.mailreceiverMail");
//        List<Map<String, String>> sendingMailTargetList = schedulerMapper.getSendingMailTargetList();
//
//        for (Map<String, String> target : sendingMailTargetList) {
//
//            if(devFlag) {
//                target.put("RECV_EMAIL", mailAddrForDev);
//            }
//
//            MimeMessage mimeMessage = javaMailSender.createMimeMessage();
//            MimeMessageHelper mimeMessageHelper = new MimeMessageHelper(mimeMessage, false);
//            mimeMessageHelper.setFrom(new InternetAddress(target.get("SEND_EMAIL"), target.get("SEND_USER_NM")));
//            mimeMessageHelper.setReplyTo(new InternetAddress(target.get("SEND_EMAIL"), target.get("SEND_USER_NM")));
//            mimeMessageHelper.setTo(new InternetAddress(target.get("RECV_EMAIL"), target.get("RECV_USER_NM")));
//            mimeMessageHelper.setSubject(target.get("SUBJECT"));
//            mimeMessageHelper.setText(largeTextService.selectLargeText(target.get("MAIL_TEXT_NUM")));
//            mimeMessageHelper.setSentDate(new Date());
//
//            javaMailSender.send(mimeMessageHelper.getMimeMessage());
//            schedulerMapper.updateMailFlagAsSent(target);
//        }
//    }
}

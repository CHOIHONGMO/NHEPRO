<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.st_ones.batch.nhebatch.UserMig_Mapper">



	<select id="targetCustList" parameterType="hashmap" resultType="hashMap">
		SELECT DISTINCT A.CORP_NO
		FROM TB_CO_CORP A
		--WHERE A.NH_CORP_CODE = '2'
	</select>



	<select id="targetUserList" parameterType="hashmap" resultType="hashMap">
		SELECT DISTINCT A.USER_ID FROM TB_CO_USER A
		WHERE EXISTS (
			SELECT '1' FROM TB_CO_DEPT
			WHERE CORP_NO = A.CORP_NO
			AND DEPT_C= A.DEPT_C
		)
	</select>


</mapper>